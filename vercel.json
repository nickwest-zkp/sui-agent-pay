{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "if [ -f front/agentpayapp/package.json ]; then pnpm --dir front/agentpayapp install --no-frozen-lockfile; else pnpm install --no-frozen-lockfile; fi",
  "buildCommand": "if [ -f front/agentpayapp/package.json ]; then pnpm --dir front/agentpayapp build; else pnpm build; fi"
}
